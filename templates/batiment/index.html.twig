{% extends 'base.html.twig' %}

{% block title %}batiment{% endblock %}

{% block body %}
    <h1>Ajouter un batiment </h1>
    {{ form_start(form)}}
    <div class="form-group">
        {{form_widget(form)}}
        <input type="submit" value="Validez">
    </div>
    {{ form_end(form)}}

         <h3><a href={{ path('batiment_afficher') }}>Liste des Batmient</a></h3>
{% endblock %}
